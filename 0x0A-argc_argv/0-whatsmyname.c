#include <stdio.h>
#include <stdio.h>
#include <string.h>
/**
*main - prints the name of the program
*@argc: number of arguments to be passed to a function
*@argv: argument vector of pointers to string
*Return: 0
*/
int  main(int argc __attribute__((unused)), char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
	}
